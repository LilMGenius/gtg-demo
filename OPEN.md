# 대기 중인 빨간 게이트 이름

러너가 읽는 목록이다. 빨간 게이트가 여기 있으면 알려진 대기 건이고, 없으면 새 소식이라 커밋 로그에 그렇게 찍힌다.

판단 근거와 갈래는 이 파일이 안 갖는다. 정본은 `daedal-games` 레포 `feat/gtg`의 `docs/gtg/OPEN.md`다. 같은 글이 두 레포에 있으면 한쪽이 조용히 낡고, 낡은 쪽을 읽은 랩이 이미 답이 난 질문을 다시 연다.

여기 이름이 있다고 그 게이트를 초록으로 만들어도 되는 것은 아니다. 그 반대다. 답이 오기 전에는 손대지 않는다.

- `gear-effect` 장갑 값당. 판정식의 어느 항에 얹을까
- `bot-effect` 봇 1등급이 파는 것. 판정식의 어느 항에 얹을까
- `lategear` 만렙 뒤에 장비가 파는 것. 판정식의 어느 항에 얹을까
- `rapport` 만렙에서 라포가 판정에 못 닿는다. 판정식의 어느 항에 얹을까
- `offball` 오프더볼 교환. 판정식의 어느 항에 얹을까
- `corr` 오프더볼이 세이브 경로 칸 중 유일한 음수. `offball`과 한 답이다
- `balance` 성장 곡선 목표를 어디에 둘까
- `idle` 방치가 벌이 되는 구조를 의도로 볼까
- `buff` 자양강장제 선반 문구가 표본에 따라 갈린다
- `crowd` 동네 등급의 화면 가시 인원. 지금은 초록이라 이 목록의 대조군이다

