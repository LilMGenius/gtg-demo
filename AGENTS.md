# GTG 코드 레포

3D 골키퍼 키우기의 실행 코드다. 무엇을 만드는지는 기록 레포의 `docs/gtg/PLAN.md`가 소유하고, 이 파일은 코드가 어떤 경계로 나뉘고 무엇을 건드리면 안 되는지를 소유한다.

## 경계

판정과 렌더를 섞지 않는다. 롤이 먼저 굴러 결과를 확정하고 연출은 확정된 결과를 연기한다. 물리엔진이 없는 이유는 성능이 아니라 원인의 소유권이다. 모든 사고는 스탯 하나에 귀속되고 원인 없는 난수는 판정에 없다.

| 도메인 | 폴더 | 소유 |
| --- | --- | --- |
| 판정 | `src/` | 한 구의 결과, 스탯 원장, 선수 명단 |
| 상태 | `web/src/state/` | 저장되는 값. 지갑, 장비, 봇, 버프, 라포, 행인, 만남 |
| 렌더 | `web/src/render/` | 장면, 카메라, 배우, 임팩트, 절차적 텍스처 |
| 화면 | `web/src/ui/`, `web/src/main.mjs` | HUD, 패널, 자막, 입력 배선 |
| 소리 | `web/src/audio/` | 브금, 효과음, 볼륨 |
| 계기 | `tools/` | 게이트와 진단 드라이버 |

판정은 브라우저를 모른다. `src/`의 어떤 파일도 `document`나 `window`를 읽지 않으므로 node에서 그대로 돌고, 그래서 게이트가 수천 판을 시드로 재현할 수 있다. 이 성질을 깨는 import 하나가 판정 게이트 전체를 브라우저 의존으로 만든다.

상태는 판정 밖에서 판정에 인자로 들어간다. 장비 등급은 `resolve`의 입력이고 라포는 `gazeAid` 승수이며 봇은 키퍼 사본의 판단력을 갈아끼운다. 어느 것도 판정식 안에 새 롤을 만들지 않는다. 새 `rng()` 호출을 하나라도 늘리면 그 뒤 모든 구가 밀려 shot과 band와 save와 pose 네 게이트가 통째로 흔들린다.

보조 수단이 후반에 어떻게 되는지는 그것이 판정식의 어느 자리에 얹혔는지가 정한다. 감산항에 얹힌 것은 스탯이 그 항을 0으로 밀면 같이 죽는다. 몸싸움 10에서 `carryP`가 음수가 되어 유니폼이 뺄 것을 잃고, 핸들링 5에서 `gloveP`가 통째로 닫힌다. 승산항에 얹힌 것은 반대로 스탯이 클수록 같이 커진다. 자양강장제는 만렙에서 56에서 337로 여섯 배가 되고, 팔로워 배수인 바이럴 떡밥과 동네도 같이 커진다. 새 상품을 어느 자리에 얹을지가 그 상품의 수명을 정한다.

## 판정

`src/chain.mjs`가 한 구를 판정한다. 한 구는 단계 넷에서 끊기고 롤은 여섯까지 굴러간다. 같은 단계의 사고들은 새 롤을 뽑지 않고 롤 하나를 구간으로 나눠 가른다. 새 롤을 뒤면 한 구가 일곱 번 굴러 시드 재현성이 깨진다.

`src/ledger.mjs`가 스탯 원장이다. `GROWABLE` 열다섯 칸이 훈련으로 오르고 `HIDDEN` 둘은 안 보인다. 이 배열의 순서는 훈련장과 내 정보 두 화면의 표시 순서이기도 하다.

`src/roster.mjs`가 키커와 키퍼 명단이다. 여기나 `src/chain.mjs`를 건드리면 shot과 band와 save와 pose 네 게이트를 전부 다시 돌린다. 이름 하나만 고쳐도 `buildSet`이 뽑는 순서가 바뀐다.

`K_LAT`과 `K_DIVE`는 손대지 않는다. 세이브율 전체가 이 둘 위에 서 있어서 한 자리만 움직여도 밸런스 게이트 여러 개가 동시에 색을 바꾼다.

## 상태

저장은 `web/src/state/save.mjs`의 키 하나에 전부 들어간다. 복원은 방어적으로 클램프한다. 반쪽 상태를 살려 두면 다음 저장에서 나머지가 지워진다.

주입은 `web/src/state/inject.mjs`다. `?preset=` 으로만 켜지고 판정식을 안 건드리며 저장이 정상적으로 도달할 수 있는 상태를 앞당길 뿐이다. 한 프리셋은 한 축만 움직인다. 두 축을 같이 밀면 어느 쪽이 화면을 바꿨는지 게이트가 못 가른다. 도달 불가능한 값을 넣으면 그 표본으로 잰 수치는 게임의 수치가 아니다.

등급이 있는 상품은 `web/src/state/gear.mjs`의 `SHELVES` 하나가 여덟 선반을 전부 그린다. 등급은 순서대로 살 필요가 없고 등급 r의 지출은 그 행의 값 하나다. 봇과 버프는 등급이 아니라 시간을 사므로 그 덩어리에 안 들어가고 별도 렌더러를 갖는다.

봇은 성장은 남기고 화제는 안 남긴다. 봇이 뛴 구는 팔로워가 0이고 라포도 안 쌓인다. 순수 상승이 아니라 교환이고, `state.botRan` 한 칸이 그 둘을 지배한다.

라포와 만남은 판정식 밖에서만 움직인다. 라포는 `gazeAid` 승수로 들어가고 만남은 라포 숫자와 지갑과 팔로워만 바꾼다. 만남의 굴림이 판정용 rng를 쓰면 그 뒤 모든 구가 밀린다.

## 렌더

카메라는 골대 뒤에서 앞을 본다. 월드 +x가 화면 왼쪽이다. 뒷기둥이 프레임 밖으로 나가는 것은 결함이 아니라 그 자리에서 보는 그림이다.

골대는 직육면체다. 화면 폭에 맞추려고 기하를 좁히지 않는다. 프레이밍 문제는 카메라로 풀고 실물 형상은 실물 형상으로 둔다.

공을 덮는 층은 스텐실로 비킨다. 플래시의 `depthTest:false`는 의도이고 그물의 `depthWrite:false`도 손대지 않는다. 가림은 존재의 문제가 아니라 순서의 문제라 그리는 쪽이 아니라 겹치는 쪽에서 잰다.

`CAM_BASE`와 `CAM_LOOK`과 `BASE_FOV`는 손대지 않는다. 화각을 건드리면 배우 크기와 골대 프레이밍과 임팩트 위치가 한꺼번에 움직인다.

상품을 사면 겉모습이 변한다. 장갑과 유니폼과 양말과 골대와 타투와 경기장이 대상이고, 착용 상태는 키퍼마다 따로 산다. 값이 다른데 화면이 같으면 그 둘은 값만 다른 같은 물건이다.

## 화면

HUD와 패널은 DOM이다. 캔버스는 경기장만 그린다. 패널이 열려 있는 동안에도 판은 계속 돈다.

화면에 없는 시스템은 만들지 않는다. 빈 탭도 만들지 않는다. 못 누르는 버튼은 사유를 자기 글자로 말한다.

패널 스타일 규칙은 아이디 스코프라 상속되지 않는다. 새 패널을 세우면 닫기와 배경을 그 패널 이름으로 다시 적는다. 안 적으면 브라우저 기본 버튼이 뜬다.

의역은 아이콘까지 가야 끝난다. 뜻이 한국어로 서 있는지와 그 뜻이 그림으로도 서는지는 다른 축이다.

진입점은 장르 표준을 따른다. 자기 정보는 초상화로 열고, 재화를 누르면 충전과 획득 경로가 열리고, 상점은 제 버튼을 갖는다. 표준을 벗어난 진입점은 눌러 볼 생각 자체가 안 든다.

폰트는 용도별로 두셋을 전역 스타일에 정의하고 그것만 쓴다. 제목용과 본문용과 수치용이다. 굵은 하나로 다 쓰면 강조가 사라지고 장문이 먼저 읽기를 잃는다.

상품 선반은 글자가 아니라 그림이 채운다. 썸네일과 시착용이 없으면 그것은 상점이 아니라 목록이다. 나열은 한 열이 아니라 뷰포트 폭을 쓰는 격자다.

판을 세는 단위로 구를 쓰지 않는다. 직역이라 한국어로 안 읽힌다.

## 소리

공 차기와 골대 튕김과 드리블과 공 놓기와 발소리를 현실적으로 낸다. 현실적이게 못 하겠으면 넣지 않는다. 어설픈 소리는 없는 것보다 나쁘다.

## 계기

게이트는 `tools/<이름>-gate.mjs`이고 통과와 실패를 스스로 출력한다. 절대경로로만 만든다. 상대경로로 만들면 엉뚱한 레포 루트에 떨어져 추적되지 않고, 한 번도 안 돈 게이트는 빨간불도 파란불도 내지 않아 어떤 보고에도 안 나타난다.

문턱은 낮추지 않는다. 실측이 문턱 아래면 대상을 고치고, 못 고치면 실패로 두고 보고한다.

0을 재는 축은 대조군이 양수를 내야 비로소 설계다. 대조군이 없으면 끊긴 배선과 같은 그림이다. 표본 수가 0인 대조군은 통과가 아니라 계기 사망이다.

하한이나 상한에 붙어 있는 값을 재는 축은 표본이 그 경계에서 떨어져 있는지를 먼저 본다. 팔로워는 0에서 시작하고 아래로 클램프되므로 신규 저장에서는 잃는 쪽이 구조적으로 관측되지 않는다.

화면 축은 문자열이 DOM에 있는 것과 화면에 보이는 것을 구분하지 않는다. `textContent`만 읽는 축에는 그 한계를 이름에 적거나 가시성을 같이 잰다.

게이트는 순차로 돌린다. 동시에 돌리면 시간을 재는 게이트가 자기 측정 창 안에서 죽어 거짓 빨간불을 낸다.

빨간 게이트가 전부 결함은 아니다. 실측이 끝나고 다음 한 걸음이 상수나 문턱을 건드리는 자리면 답을 기다린다. 거기 적힌 게이트를 초록으로 만드는 것은 답이 아니다. 쓸기는 매번 빨간 게이트가 [OPEN.md](./OPEN.md)에 있는지 대조하고, 없으면 새 소식으로 알린다.

그 파일에는 게이트 이름만 적는다. 실측과 갈래와 판단 근거는 지식 레포 https://github.com/DaedalGames/daedal-games 의 `feat/gtg` 브랜치가 소유한다. 여기에 근거를 같이 쓰면 두 곳이 같은 글을 들고 있다가 한쪽이 낡고, 낡은 쪽을 읽은 랩이 이미 답이 난 질문을 다시 연다.

계기는 브라우저를 여는 것과 시드만 돌리는 것 둘로 나뉘지만 폴더는 하나다. 판정 계기를 따로 두면 게이트를 셀 때 한쪽이 빠지고, 빠진 쪽은 아무도 안 돌린다. `cause-gate`와 `physique-gate`는 원인 귀속과 체격 방향을 재고 `growth-gate`와 `tempo-gate`는 성장과 회전이 화면을 바꾸는지 재는 트립와이어다. 넷 다 브라우저 없이 돈다.
